MEMORY {
    RAM:    start = $3000,  size = $5000, type = rw;
    ROM:    start = $8000,  size = $8000, file = %O, fill = yes;
}

SEGMENTS {
    CODE:     load = ROM, type = ro,  define = yes;
    VECTORS:  load = ROM, type = ro,  start = $FFFA;
}
